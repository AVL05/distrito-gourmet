# Dejar vacío para usar /api relativo (Docker/Nginx/Vercel demo).
# En desarrollo local con Vite separado, usar el origen del backend Laravel.
# Ejemplo: VITE_API_URL=http://127.0.0.1:8000
VITE_API_URL=
VITE_DEMO_MODE=false
